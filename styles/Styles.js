import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  dropShadow: {
    shadowColor: '#000',
    shadowOffset: {
      width: 1,
      height: 1,
    },
    shadowOpacity: 0.25,
    shadowRadius: 2,
    elevation: 3,
  },
});

export default Styles;
